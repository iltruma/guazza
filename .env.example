# Guazza — template variabili d'ambiente
# Copia in .env e compila con i valori reali. NON committare .env.

# ── Netatmo OAuth2 (job realtime) ─────────────────────────────────────────────
# Credenziali app: https://dev.netatmo.com/apps
NETATMO_CLIENT_ID=
NETATMO_CLIENT_SECRET=
NETATMO_ACCESS_TOKEN=
NETATMO_REFRESH_TOKEN=

# ── Monitoring ────────────────────────────────────────────────────────────────
# URL base push Uptime Kuma (monitor Push, dead-man switch). Se vuoto, il push è saltato.
# Per-job: KUMA_PUSH_URL_{JOB_NAME_UPPER} (es. KUMA_PUSH_URL_FORECAST) ha la precedenza.
KUMA_PUSH_URL=
KUMA_PUSH_URL_MONITOR=   # canale separato per drift ACI (status=down)

# ── Storage ───────────────────────────────────────────────────────────────────
# Path del file DuckDB. Default: /var/lib/guazza/guazza.duckdb
DB_PATH=

# Directory dei file YAML di configurazione. Default: <repo>/config
CONFIG_DIR=
